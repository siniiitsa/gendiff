name: Node CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: npm install, build
        run: |
          make install
          make build
      - name: run linter
        run: make lint
      - name: run tests
        run: make test
